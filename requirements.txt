# Core ML / data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.11
xgboost>=1.7,<2.0  # shap is incompatible with xgboost>=2.0 (base_score parsing bug)

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Deep learning (pick ONE backend — TF or PyTorch)
torch>=2.1
tensorflow>=2.13

# NLP
spacy>=3.7
transformers>=4.35
tokenizers>=0.15
nltk>=3.8

# XAI tools
lime>=0.2
shap>=0.45
anchor-exp>=0.0.2
dice-ml>=0.11
PyALE>=1.2

# RuleFit (uses H2O)
h2o>=3.42

# MERLIN (optional — requires system-level espresso)
# merlin-xai>=0.1
# pyeda>=0.29

# Image
scikit-image>=0.22
Pillow>=10.0

# Spacy model (install separately)
# python -m spacy download en_core_web_sm
